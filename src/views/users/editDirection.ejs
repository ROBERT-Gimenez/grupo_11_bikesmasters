<!DOCTYPE html>
<html lang="en">
<head>

    <%- include('../partials/head') %> 

</head>
<body>

    <%- include('../partials/header') %> 
    
    <main>
        <div class="register">
        <div id="head-logo">
            <h2>Editar dirección</h2>
        </div>
        <form id="formulario__direction" class="form-register" action="/usuario/perfil/editar/<%= session.user.id %>?_method=PUT" method="post">
            <div class="register-input">
                <label for="direccion">Dirección</label>
                <input type="text" name="direccion" id="direccion" class="input-style" placeholder="Ingrese su calle" value="<%= direccion.direccion %>">
                <small class="text-danger" id="inputDireccionError"><%= (locals.errors && errors.direccion)? errors.direccion.msg : "" %></small>
            </div>
            <div class="register-input">
                <label for="altura">Altura</label>
                <input type="number" name="altura" id="altura" class="input-style" placeholder="Ingrese la altura" value="<%= direccion.altura %>">
                <small class="text-danger" id="inputAlturaError"><%= (locals.errors && errors.altura)? errors.altura.msg : "" %></small>
            </div>
            
            <div class="register-input">
                <label for="provincia">Provincia</label>
                <select name="provincia" id="provincia" class="input-style">
                    <option value="<%= direccion.provincia %>"><%= direccion.provincia %></option>
                </select>
                <small class="text-danger" id="provinciaError"><%= (locals.errors && errors.provincia)? errors.provincia.msg : "" %></small>
            </div>
            <div class="register-input">
                <label for="localidad">Localidad</label>
                <select name="localidad" id="localidad" class="input-style">
                    <option value="<%= direccion.localidad %> "><%= direccion.localidad %></option>
                </select>
                <small class="text-danger" id="localidadError"><%= (locals.errors && errors.localidad)? errors.localidad.msg : "" %></small>
            </div>
            
            <div class="register-input">
                <label for="postal">Código postal</label>
                <input type="number" name="postal" id="postal" class="input-style" placeholder="Código postal" value="<%= direccion.codigo_postal %>">
                <small class="text-danger" id="inputPostalError"><%= (locals.errors && errors.postal)? errors.postal.msg : "" %></small>
            </div>
            
            <div class="btn_register" id="submit__direction">
                <button class="btn-submit_register" type="submit">Guardar</button>
                <small class="text-danger" id="submitError"></small>
                <span class="admin"><a class="admin" href="/usuario/perfil/<%= session.user.id %> ">Volver</a></span>
            </div>
        </form>
        </div>
    </main> 

    <%- include('../partials/footer') %> 
    <script src="/js/editDirectionValidation.js"></script>    
</body>
</html>